import EmbedModal from './toolbar/embedURLInputModal';

const Modals = {
  EMBED_URL_INPUT: 'embed-url-input',
};

const ModalsMap = {
  [Modals.EMBED_URL_INPUT]: EmbedModal,
};

export { Modals, ModalsMap };
